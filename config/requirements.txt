# Core Dependencies
playwright>=1.40.0
yt-dlp>=2023.0.0

# Web GUI Dependencies
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
eventlet>=0.33.0
requests>=2.31.0

# System monitoring (used for memory-based parallelism limiting)
psutil>=5.9.0

# Optional: Image optimization for cover caching (WebP conversion)
Pillow>=10.0.0

# Optional
python-dotenv>=1.0.0
